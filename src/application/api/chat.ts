import { handleMessage, handleMessageStreaming } from "../agents/orchestrator";
import type { Message } from "../agents/types";

export { handleMessage, handleMessageStreaming };
export type { Message };
